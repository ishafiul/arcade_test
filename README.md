# run

```shell
dart run --enable-vm-service
```
### with `arcade_cli`:
```shell
arcade serve
```

### with `arcade_cli` watch mode:
```shell
arcade serve --watch
```
### docker
```shell
docker-compose up
```